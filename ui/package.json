{
  "scripts": {
    "start": "lerna exec --parallel -- yarn start",
    "bootstrap": "lerna bootstrap",
    "test:watch": "lerna exec --parallel -- yarn test:watch"
  },
  "devDependencies": {
    "lerna": "^3.15.0"
  },
  "name": "root",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ]
}
